#include <stdio.h>

/**
 * main - print single digit in base 10 starting from 0
 * followed by new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (i = n; n < 10; n++)
	{
		printf("%d", n);
		printf("\n");
	}
	return (0);
}
